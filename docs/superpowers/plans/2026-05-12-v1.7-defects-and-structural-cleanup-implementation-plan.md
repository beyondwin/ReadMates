# ReadMates v1.7.0 후속 결함·논리·구조 정리 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 2026-05-12 v1.7 followup hardening 머지 이후 심층 감사에서 검증된 13개 항목(결함 위험 5건, 논리 문제 4건, 구조 개선 4건)을 한 사이클로 정리한다. 사용자 가시 동작은 보존하면서 보안/감사 무결성, 클라이언트 권한 가드, 시크릿/권한/manifest 합성 로직 중복을 닫는다.

**Architecture:** 서버 코드는 `adapter.in.web → application.port.in → application.service → application.port.out → adapter.out.persistence` 경계 유지. 권한 합성은 application service로, BFF 시크릿 비교는 공통 utility로, deploy ledger는 NDJSON schema로 집약. 클라이언트는 명시 상태 머신(`session_expired`)과 reentrancy guard로 무한 redirect를 막는다.

**Tech Stack:** Kotlin, Spring Boot, JUnit 5, ArchUnit, Micrometer, React/Vite, Cloudflare Pages Functions, Bash, jq, Flyway(미사용), GitHub Actions.

---

## Source Documents

- Detailed implementation document: `docs/superpowers/specs/2026-05-12-v1.7-defects-and-structural-cleanup-detailed-implementation.md`
- 사전 sibling plan(이미 머지됨): `docs/superpowers/plans/2026-05-12-v1.7-followup-hardening-implementation-plan.md`
- 같은 사이클 follow-up(코드 변경 별도): `docs/superpowers/plans/2026-05-12-persistence-web-cleanup-implementation-plan.md`
- 아키텍처 출처: `docs/development/architecture.md`, `docs/development/adr/0013-bff-host-header-policy.md`
- Surface guides: `docs/agents/server.md`, `docs/agents/front.md`, `docs/agents/docs.md`

## File Map

### Create

- `docs/development/adr/0014-bff-secret-rotation-lifecycle.md`: BFF 다중 secret 회전 단계와 BFF/server 배포 순서.
- `docs/development/adr/0015-notification-outbox-dedupe-policy.md`: 이벤트별 dedupeKey 정책표.
- `docs/development/adr/0016-deploy-ledger-event-schema.md`: deploy ledger NDJSON schema.
- `server/src/main/kotlin/com/readmates/shared/security/SecretComparator.kt`: timing-uniform 시크릿 비교 helper.
- `server/src/main/kotlin/com/readmates/shared/security/ClientIpHashingProperties.kt`: IP-hash 정책 properties(빈 secret 강제).
- `server/src/main/kotlin/com/readmates/auth/domain/MemberLifecycleStatus.kt`: 멤버 lifecycle enum + 허용 전이.
- `server/src/main/kotlin/com/readmates/auth/application/service/AuthoritySynthesisService.kt`(인터페이스) + `DefaultAuthoritySynthesisService.kt`(구현).
- `server/src/test/kotlin/com/readmates/shared/security/SecretComparatorTest.kt`.
- `server/src/test/kotlin/com/readmates/shared/security/ClientIpHashingPropertiesTest.kt`.
- `server/src/test/kotlin/com/readmates/auth/domain/MemberLifecycleStatusTest.kt`.
- `server/src/test/kotlin/com/readmates/auth/application/service/DefaultAuthoritySynthesisServiceTest.kt`.
- `front/functions/api/bff/__internal/secret-status.ts`: BFF secret 진단 라우트.
- `front/tests/unit/cloudflare-bff-secret-status.test.ts`: 위 라우트 테스트.
- `scripts/fixtures/public-release-candidate-coverage.txt`: top-level 디렉토리 fixture.

### Modify

- `CHANGELOG.md`: `## Unreleased` 섹션 — 운영자 가시 변화(메트릭, env var, ledger schema, secret rotation 단계) 한 줄씩.
- `server/src/main/kotlin/com/readmates/auth/infrastructure/security/BffSecretAuditExecutorConfig.kt`: shutdown wait + 메트릭.
- `server/src/main/kotlin/com/readmates/auth/infrastructure/security/BffSecretFilter.kt`: SecretComparator 사용.
- `server/src/main/kotlin/com/readmates/auth/infrastructure/security/RateLimitFilter.kt`: SecretComparator 사용.
- `server/src/main/kotlin/com/readmates/auth/infrastructure/security/MemberAuthoritiesFilter.kt`: KDoc + AuthoritySynthesisService 위임.
- `server/src/main/kotlin/com/readmates/shared/security/ClientIpHashing.kt`: `requireNonBlankSecret` 파라미터.
- `server/src/main/kotlin/com/readmates/notification/application/service/NotificationDispatchService.kt`: null status retryable + 메트릭.
- `server/src/main/kotlin/com/readmates/notification/application/service/NotificationEventService.kt`: dedupeKey KDoc.
- `server/src/main/kotlin/com/readmates/auth/adapter/out/persistence/JdbcMemberLifecycleStoreAdapter.kt`: enum 기반 사전 검증.
- `server/src/main/resources/application.yml`, `application-test.yml`: ip-hash properties 키.
- `server/src/test/kotlin/com/readmates/auth/infrastructure/security/MemberAuthoritiesFilterTest.kt`: 4 시나리오 테스트.
- `front/src/app/auth-state.ts`: `session_expired` variant.
- `front/src/app/auth-context.tsx`: 401/네트워크/5xx 분기.
- `front/src/app/route-guards.tsx`: clubSlug bypass 제거.
- `front/shared/api/client.ts`: redirect reentrancy guard.
- `front/tests/unit/auth-context.test.tsx`: 401 분기 테스트.
- `front/tests/unit/route-guards.test.tsx`(또는 신규): 권한 가드 매트릭스.
- `front/tests/unit/client-redirect-guard.test.ts`(또는 기존 client 테스트 보강): redirect guard 검증.
- `front/functions/_shared/proxy.ts`: rotation stage helper + fingerprint helper.
- `deploy/oci/05-deploy-compose-stack.sh`: ledger schema helper.
- `deploy/oci/watch-compose-post-deploy.sh`: 동일 helper 도입.
- `deploy/oci/01-vm-setup.sh`: jq 설치 보장.
- `scripts/build-public-release-candidate.sh`: deny-list 우선 모델.
- `scripts/verify-public-release-fixtures.sh`: coverage fixture diff.
- `docs/development/adr/README.md`(있다면): ADR-0014/15/16 등재.

### Verify

- `server/src/test/kotlin/com/readmates/architecture/ServerArchitectureBoundaryTest.kt`: baseline exception 변경 없이 통과.

---

## Task 1: Preflight Scope Check

**Files:**
- Read only: 저장소 상태와 타겟 파일.

- [ ] **Step 1: 분기/작업 트리 상태**

```bash
git status --short --branch
git log --oneline 55e1be1..HEAD | head -5
```

기대: `main`이 `55e1be1`에 있고 working tree clean(또는 worktree 격리에서 시작).

- [ ] **Step 2: 결함 위치 현재 사실 재확인**

```bash
rg -n 'setWaitForTasksToCompleteOnShutdown' server/src/main/kotlin/com/readmates/auth/infrastructure/security/BffSecretAuditExecutorConfig.kt
rg -n 'baseSecret' server/src/main/kotlin/com/readmates/shared/security/ClientIpHashing.kt
rg -n 'if (clubSlug)' front/src/app/route-guards.tsx
rg -n 'window.location.assign\|loginPathForReturnTo' front/shared/api/client.ts
rg -n 'IllegalStateException' server/src/main/kotlin/com/readmates/notification/application/service/NotificationDispatchService.kt
rg -n 'dedupeKey' server/src/main/kotlin/com/readmates/notification/application/service/NotificationEventService.kt
rg -n 'remote_ledger_append' deploy/oci/05-deploy-compose-stack.sh | head -5
```

기대: 모든 grep이 spec과 일치하는 위치를 보여줌.

---

## Task 2: DEF-001 — BFF audit executor graceful shutdown

**Files:**
- Modify: `server/src/main/kotlin/com/readmates/auth/infrastructure/security/BffSecretAuditExecutorConfig.kt`
- Test: `server/src/test/kotlin/com/readmates/auth/infrastructure/security/BffSecretAuditExecutorConfigTest.kt`(신규)

- [ ] **Step 1: 신규 단위 테스트 추가**

`server/src/test/kotlin/com/readmates/auth/infrastructure/security/BffSecretAuditExecutorConfigTest.kt`:

```kotlin
package com.readmates.auth.infrastructure.security

import io.micrometer.core.instrument.simple.SimpleMeterRegistry
import org.assertj.core.api.Assertions.assertThat
import org.junit.jupiter.api.Test
import java.util.concurrent.CountDownLatch
import java.util.concurrent.TimeUnit

class BffSecretAuditExecutorConfigTest {

    @Test
    fun `executor waits for in-flight tasks on shutdown`() {
        val registry = SimpleMeterRegistry()
        val config = BffSecretAuditExecutorConfig(registry)
        val executor = config.bffSecretAuditExecutor()
        val started = CountDownLatch(1)
        val finished = CountDownLatch(1)

        executor.execute {
            started.countDown()
            Thread.sleep(200)
            finished.countDown()
        }
        started.await(1, TimeUnit.SECONDS)

        executor.shutdown()

        assertThat(finished.await(2, TimeUnit.SECONDS)).isTrue
    }

    @Test
    fun `rejected tasks increment dropped counter`() {
        val registry = SimpleMeterRegistry()
        val config = BffSecretAuditExecutorConfig(registry)
        val executor = config.bffSecretAuditExecutor()

        executor.threadPoolExecutor.shutdownNow()
        executor.execute { /* should be rejected */ }

        val dropped = registry.get("bff.audit.shutdown.dropped").counter().count()
        assertThat(dropped).isGreaterThanOrEqualTo(1.0)
    }
}
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
./server/gradlew -p server test --tests 'com.readmates.auth.infrastructure.security.BffSecretAuditExecutorConfigTest'
```

기대: FAIL (현재 생성자에 `MeterRegistry` 없음, shutdown wait 미설정).

- [ ] **Step 3: `BffSecretAuditExecutorConfig.kt` 교체**

`server/src/main/kotlin/com/readmates/auth/infrastructure/security/BffSecretAuditExecutorConfig.kt`:

```kotlin
package com.readmates.auth.infrastructure.security

import io.micrometer.core.instrument.Counter
import io.micrometer.core.instrument.MeterRegistry
import org.springframework.context.annotation.Bean
import org.springframework.context.annotation.Configuration
import org.springframework.scheduling.concurrent.ThreadPoolTaskExecutor

@Configuration
class BffSecretAuditExecutorConfig(
    private val meterRegistry: MeterRegistry,
) {
    @Bean("bffSecretAuditExecutor")
    fun bffSecretAuditExecutor(): ThreadPoolTaskExecutor {
        val droppedCounter: Counter = Counter.builder("bff.audit.shutdown.dropped")
            .description("BFF audit tasks rejected because the executor was shutting down or its queue was full")
            .register(meterRegistry)

        return ThreadPoolTaskExecutor().apply {
            corePoolSize = 1
            maxPoolSize = 2
            queueCapacity = 1_000
            setThreadNamePrefix("bff-secret-audit-")
            setWaitForTasksToCompleteOnShutdown(true)
            setAwaitTerminationSeconds(5)
            setRejectedExecutionHandler { _, _ -> droppedCounter.increment() }
            initialize()
        }
    }
}
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
./server/gradlew -p server test \
  --tests 'com.readmates.auth.infrastructure.security.BffSecretAuditExecutorConfigTest' \
  --tests 'com.readmates.auth.infrastructure.security.BffSecretFilterAuditTest'
```

기대: 모두 PASS.

- [ ] **Step 5: Slice commit**

```bash
git add server/src/main/kotlin/com/readmates/auth/infrastructure/security/BffSecretAuditExecutorConfig.kt \
        server/src/test/kotlin/com/readmates/auth/infrastructure/security/BffSecretAuditExecutorConfigTest.kt
git commit -m "fix: flush bff audit executor on shutdown"
```

---

## Task 3: DEF-002 — ClientIpHashing baseSecret 필수화

**Files:**
- Create: `server/src/main/kotlin/com/readmates/shared/security/ClientIpHashingProperties.kt`
- Modify: `server/src/main/kotlin/com/readmates/shared/security/ClientIpHashing.kt`
- Modify: `server/src/main/kotlin/com/readmates/auth/infrastructure/security/BffSecretFilter.kt`(생성자 파라미터)
- Modify: `server/src/main/kotlin/com/readmates/auth/infrastructure/security/RateLimitFilter.kt`(생성자 파라미터)
- Modify: `server/src/main/resources/application.yml`, `application-test.yml`
- Test: `server/src/test/kotlin/com/readmates/shared/security/ClientIpHashingPropertiesTest.kt`(신규)

- [ ] **Step 1: properties 클래스 작성**

`server/src/main/kotlin/com/readmates/shared/security/ClientIpHashingProperties.kt`:

```kotlin
package com.readmates.shared.security

import org.springframework.boot.context.properties.ConfigurationProperties
import org.springframework.core.env.Environment

@ConfigurationProperties(prefix = "readmates.security.ip-hash")
data class ClientIpHashingProperties(
    val baseSecret: String = "",
    val allowEmptySecret: Boolean = false,
) {
    fun validate(environment: Environment) {
        if (baseSecret.isNotBlank()) return
        val productionLike = environment.activeProfiles.let { profiles ->
            profiles.isEmpty() || profiles.any { it.equals("production", ignoreCase = true) }
        }
        if (productionLike && !allowEmptySecret) {
            throw IllegalStateException(
                "readmates.security.ip-hash.base-secret (env READMATES_IP_HASH_BASE_SECRET) " +
                    "must be set in production. Set readmates.security.ip-hash.allow-empty-secret=true " +
                    "explicitly to bypass in non-production profiles."
            )
        }
    }
}
```

- [ ] **Step 2: properties 검증 테스트**

`server/src/test/kotlin/com/readmates/shared/security/ClientIpHashingPropertiesTest.kt`:

```kotlin
package com.readmates.shared.security

import org.assertj.core.api.Assertions.assertThat
import org.assertj.core.api.Assertions.assertThatThrownBy
import org.junit.jupiter.api.Test
import org.springframework.mock.env.MockEnvironment

class ClientIpHashingPropertiesTest {

    @Test
    fun `production profile rejects blank base secret`() {
        val env = MockEnvironment().also { it.setActiveProfiles("production") }
        val props = ClientIpHashingProperties(baseSecret = "", allowEmptySecret = false)
        assertThatThrownBy { props.validate(env) }
            .isInstanceOf(IllegalStateException::class.java)
            .hasMessageContaining("READMATES_IP_HASH_BASE_SECRET")
    }

    @Test
    fun `unset profile (default to production) rejects blank base secret`() {
        val env = MockEnvironment()
        val props = ClientIpHashingProperties(baseSecret = "", allowEmptySecret = false)
        assertThatThrownBy { props.validate(env) }
            .isInstanceOf(IllegalStateException::class.java)
    }

    @Test
    fun `test profile with allowEmptySecret=true tolerates blank secret`() {
        val env = MockEnvironment().also { it.setActiveProfiles("test") }
        val props = ClientIpHashingProperties(baseSecret = "", allowEmptySecret = true)
        props.validate(env) // no throw
    }

    @Test
    fun `non blank secret passes regardless of profile`() {
        val env = MockEnvironment().also { it.setActiveProfiles("production") }
        val props = ClientIpHashingProperties(baseSecret = "secret-value", allowEmptySecret = false)
        props.validate(env) // no throw
    }
}
```

- [ ] **Step 3: 테스트 실패 확인**

```bash
./server/gradlew -p server test --tests 'com.readmates.shared.security.ClientIpHashingPropertiesTest'
```

기대: 컴파일 에러(클래스 미존재) → properties 추가 후 다시 실행 → 전부 PASS.

- [ ] **Step 4: ClientIpHashing 호출 시그니처 보강**

`server/src/main/kotlin/com/readmates/shared/security/ClientIpHashing.kt`:

```kotlin
package com.readmates.shared.security

import java.nio.charset.StandardCharsets
import java.security.MessageDigest
import java.time.Clock
import java.time.ZonedDateTime
import java.time.temporal.WeekFields

object ClientIpHashing {
    fun hashClientIp(
        raw: String?,
        baseSecret: String,
        clock: Clock = Clock.systemUTC(),
        requireNonBlankSecret: Boolean = true,
    ): String {
        val ip = raw?.takeIf { it.isNotBlank() } ?: return "anonymous"
        if (requireNonBlankSecret && baseSecret.isBlank()) {
            throw IllegalStateException(
                "ClientIpHashing.hashClientIp called with blank baseSecret while requireNonBlankSecret=true. " +
                    "Check ClientIpHashingProperties wiring."
            )
        }
        val now = ZonedDateTime.now(clock)
        val week = now.get(WeekFields.ISO.weekOfWeekBasedYear())
        val year = now.get(WeekFields.ISO.weekBasedYear())
        val salt = "${baseSecret}::${year}-W${week}"
        return MessageDigest.getInstance("SHA-256")
            .digest("$salt::$ip".toByteArray(StandardCharsets.UTF_8))
            .joinToString("") { "%02x".format(it) }
            .take(32)
    }
}
```

- [ ] **Step 5: 호출처 두 곳을 properties 기반으로 정렬**

`BffSecretFilter`와 `RateLimitFilter`의 생성자에서 `@Value("\${READMATES_IP_HASH_BASE_SECRET:}")`를 제거하고 `ClientIpHashingProperties`를 주입받도록 수정. 호출 지점에서 `properties.baseSecret`을 넘기고, properties bean이 startup에 `validate()` 한 번 호출되도록 `@PostConstruct` 또는 `ApplicationRunner` 추가.

```kotlin
// 예시: BffSecretFilter 생성자 변경 — 새 파라미터 추가
private val ipHashingProperties: ClientIpHashingProperties,
```

호출:
```kotlin
val clientIpHash = ClientIpHashing.hashClientIp(request.remoteAddr, ipHashingProperties.baseSecret)
```

- [ ] **Step 6: yml 보강**

`server/src/main/resources/application.yml` 마지막에 추가:

```yaml
readmates:
  security:
    ip-hash:
      base-secret: ${READMATES_IP_HASH_BASE_SECRET:}
      allow-empty-secret: false
```

`server/src/main/resources/application-test.yml` 마지막에 추가:

```yaml
readmates:
  security:
    ip-hash:
      base-secret: test-ip-hash-base-secret
      allow-empty-secret: false
```

- [ ] **Step 7: properties enable + validate**

`server/src/main/kotlin/com/readmates/Application.kt`(또는 `ReadMatesApplication.kt`)에 `@EnableConfigurationProperties(ClientIpHashingProperties::class)` 추가. 별도 `@Component` `ClientIpHashingValidator(ApplicationContext)`를 만들어 `@PostConstruct`에서 `properties.validate(env)` 호출.

```kotlin
// server/src/main/kotlin/com/readmates/shared/security/ClientIpHashingValidator.kt
package com.readmates.shared.security

import jakarta.annotation.PostConstruct
import org.springframework.core.env.Environment
import org.springframework.stereotype.Component

@Component
class ClientIpHashingValidator(
    private val properties: ClientIpHashingProperties,
    private val environment: Environment,
) {
    @PostConstruct
    fun validate() {
        properties.validate(environment)
    }
}
```

- [ ] **Step 8: 전체 보안 필터 테스트 통과 확인**

```bash
./server/gradlew -p server test \
  --tests 'com.readmates.shared.security.*' \
  --tests 'com.readmates.auth.infrastructure.security.BffSecretFilter*' \
  --tests 'com.readmates.auth.infrastructure.security.RateLimitFilter*'
```

기대: 모두 PASS.

- [ ] **Step 9: Slice commit**

```bash
git add server/src/main/kotlin/com/readmates/shared/security/ClientIpHashing.kt \
        server/src/main/kotlin/com/readmates/shared/security/ClientIpHashingProperties.kt \
        server/src/main/kotlin/com/readmates/shared/security/ClientIpHashingValidator.kt \
        server/src/main/kotlin/com/readmates/auth/infrastructure/security/BffSecretFilter.kt \
        server/src/main/kotlin/com/readmates/auth/infrastructure/security/RateLimitFilter.kt \
        server/src/main/resources/application.yml \
        server/src/main/resources/application-test.yml \
        server/src/test/kotlin/com/readmates/shared/security/ClientIpHashingPropertiesTest.kt
git commit -m "fix: require non-blank ip-hash base secret in production"
```

---

## Task 4: DEF-005 — NotificationDispatchService null state retryable

**Files:**
- Modify: `server/src/main/kotlin/com/readmates/notification/application/service/NotificationDispatchService.kt`
- Test: 기존 NotificationDispatchService 테스트(있으면 확장) 또는 신규

- [ ] **Step 1: 실패 재현 테스트 추가**

기존 `server/src/test/kotlin/com/readmates/notification/application/service/NotificationDispatchServiceTest.kt`는 손코딩 fake(`FakeDeliveryPort`, `RecordingMailPort`)와 `SimpleMeterRegistry`를 사용한다. 같은 패턴을 따른다. 다음 테스트를 추가한다:

```kotlin
@Test
fun `unknown delivery status is treated as retryable and counter increments`() {
    val registry = io.micrometer.core.instrument.simple.SimpleMeterRegistry()
    val deliveryPort = FakeDeliveryPort(
        deliveries = listOf(emailDelivery()),
        findDeliveryStatusOverride = { null },  // simulate UNKNOWN status
    )
    val mailPort = RecordingMailPort()
    val transactionalOps = FakeTransactionalOps(
        persistResult = listOf(emailDelivery()),
        claimEmailResult = null,  // forces handleUnclaimedEmailDelivery branch
    )
    val service = NotificationDispatchService(
        deliveryPort = deliveryPort,
        deliveryEngine = NotificationDeliveryEngine(mailPort),
        transactionalOps = transactionalOps,
        meterRegistry = registry,
    )

    assertThatThrownBy { service.dispatch(sampleMessage()) }
        .isInstanceOf(NotificationDeliveryRetryableException::class.java)
        .hasMessageContaining("UNKNOWN")

    assertThat(registry.get("notification.dispatch.unknown_status").counter().count())
        .isGreaterThanOrEqualTo(1.0)
}
```

만약 기존 `FakeDeliveryPort`에 `findDeliveryStatusOverride` 같은 훅이 없다면, 같은 파일 내에 fake를 확장하거나 `FakeDeliveryPortWithStatusOverride`라는 짧은 helper를 같은 파일에 추가한다. 새 `meterRegistry` 파라미터가 `NotificationDispatchService` 생성자에 추가되므로 기존 fake 호출도 한 줄 보강(registry 인자 전달) 필요.

- [ ] **Step 2: 테스트 실패 확인**

```bash
./server/gradlew -p server test --tests '*NotificationDispatchService*'
```

기대: `IllegalStateException`이 retryable 아니라 throw됨 → FAIL.

- [ ] **Step 3: `NotificationDispatchService` 변경**

생성자에 `meterRegistry: MeterRegistry` 추가하고, class top에 logger + counter 등록.

`handleUnclaimedEmailDelivery`의 `null` 분기를 다음으로 교체:

```kotlin
null -> {
    operationalLogger.warn(
        "Email delivery {} returned UNKNOWN status; treating as retryable",
        deliveryId,
    )
    unknownStatusCounter.increment()
    return NotificationDeliveryRetryableException(
        "Email delivery $deliveryId returned UNKNOWN status; will retry",
    )
}
```

class body 상단:

```kotlin
private val operationalLogger = org.slf4j.LoggerFactory.getLogger(javaClass)
private val unknownStatusCounter = io.micrometer.core.instrument.Counter
    .builder("notification.dispatch.unknown_status")
    .description("Email deliveries whose status was UNKNOWN at claim time")
    .register(meterRegistry)
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
./server/gradlew -p server test --tests '*NotificationDispatchService*'
```

기대: PASS.

- [ ] **Step 5: Slice commit**

```bash
git add server/src/main/kotlin/com/readmates/notification/application/service/NotificationDispatchService.kt \
        server/src/test/kotlin/com/readmates/notification/application/service/NotificationDispatchServiceTest.kt
git commit -m "fix: retry email delivery with unknown status"
```

---

## Task 5: LOGIC-004 — MemberAuthoritiesFilter null-context branch 가독성

**Files:**
- Modify: `server/src/main/kotlin/com/readmates/auth/infrastructure/security/MemberAuthoritiesFilter.kt`(KDoc)
- Modify: `server/src/test/kotlin/com/readmates/auth/infrastructure/security/MemberAuthoritiesFilterTest.kt`(4 시나리오)

- [ ] **Step 1: 4 시나리오 테스트 추가**

`MemberAuthoritiesFilterTest`에 다음 시나리오를 추가:

```kotlin
@Test
fun `slug supplied with unknown club assigns platform admin grant only`() { /* ... */ }

@Test
fun `slug supplied with unknown club assigns host support grant when present`() { /* ... */ }

@Test
fun `slug supplied with unknown club assigns no authorities for regular user`() { /* ... */ }

@Test
fun `host fallback with unknown host yields unscoped principal`() { /* ... */ }
```

각 테스트는 `requestedClubContext.supplied = true / source = SLUG / context = null`, `lookup` 결과, support grant 보유 여부를 조합해 `SecurityContextHolder.getContext().authentication.authorities` 결과를 검증.

- [ ] **Step 2: 테스트 실패 확인(혹은 부분 PASS)**

```bash
./server/gradlew -p server test --tests '*MemberAuthoritiesFilterTest*'
```

기대: 시나리오에 따라 PASS 또는 FAIL. 본 step 목표는 의도 표면화.

- [ ] **Step 3: filter class top KDoc 추가**

`MemberAuthoritiesFilter.kt`에 다음 KDoc 추가:

```kotlin
/**
 * Resolves authorities for the current principal.
 *
 * Branching rules:
 * - When [RequestedClubContext.source] is SLUG and the slug is registered, lookup the member and synthesize
 *   role + host + platform admin authorities.
 * - When [RequestedClubContext.source] is SLUG and the slug is NOT registered (`supplied=true && context=null`),
 *   the member lookup is intentionally skipped (`member=null`). Authorities are then composed entirely from
 *   platform admin + host support grants. Do NOT add a `member==null` short-circuit guard above this branch;
 *   doing so would silently strip support-grant authorities. See ADR-0013 for context.
 * - When [RequestedClubContext.source] is HOST_FALLBACK or NONE, return an unscoped principal.
 */
```

(class signature 바로 위.)

- [ ] **Step 4: 테스트 통과 확인**

```bash
./server/gradlew -p server test --tests '*MemberAuthoritiesFilterTest*'
```

기대: 4 시나리오 모두 PASS.

- [ ] **Step 5: Slice commit**

```bash
git add server/src/main/kotlin/com/readmates/auth/infrastructure/security/MemberAuthoritiesFilter.kt \
        server/src/test/kotlin/com/readmates/auth/infrastructure/security/MemberAuthoritiesFilterTest.kt
git commit -m "docs: pin member authorities filter null-context branch intent"
```

---

## Task 6: LOGIC-001 — Notification outbox dedupeKey 정책 ADR

**Files:**
- Create: `docs/development/adr/0015-notification-outbox-dedupe-policy.md`
- Modify: `server/src/main/kotlin/com/readmates/notification/application/service/NotificationEventService.kt`(메서드 KDoc)
- Modify(있으면): `docs/development/adr/README.md`

- [ ] **Step 1: ADR-0015 작성**

`docs/development/adr/0015-notification-outbox-dedupe-policy.md` 골자:

- Status: Accepted, 2026-05-12.
- Context: 이벤트별 dedupeKey 규칙이 코드 표면에 흩어져 있어 새 이벤트 추가 시 정책 결정이 일관성 없게 됨.
- Decision: 다음 표를 채택.

  | event_type | dedupeKey 구성 | 이유 | 예시 |
  | --- | --- | --- | --- |
  | feedback-document | `feedback-document:$sessionId:$documentVersion` | 같은 버전 재발행 방지. 새 버전은 다시 발송 가능 | `feedback-document:00...0301:v3` |
  | next-book | `next-book:$sessionId` | 세션당 한 번. 같은 책을 두 번 announce하지 않음 | `next-book:00...0301` |
  | review-published | `review-published:$sessionId:$authorMembershipId` | 작성자별 한 번. 같은 작성자가 다시 publish해도 한 번만 | `review-published:00...0301:00...0101` |

- Consequences: 새 이벤트 정의 시 ADR 표 보강 의무. PR 리뷰에서 dedupeKey 누락 차단.

- [ ] **Step 2: NotificationEventService 메서드 KDoc 추가**

각 이벤트 enqueue 메서드 바로 위에 KDoc 한 줄:

```kotlin
/**
 * Dedupe policy: feedback-document keys include the document version so each revision
 * triggers exactly one notification. See ADR-0015.
 */
```

(다른 두 메서드도 같은 형식.)

- [ ] **Step 3: 공개 안전 스캔**

```bash
git diff --check -- docs/development/adr/0015-notification-outbox-dedupe-policy.md \
        server/src/main/kotlin/com/readmates/notification/application/service/NotificationEventService.kt
rg -n '(/)Users/|readmates[.]kr|ocid1[.]|gh[pousr]_[A-Za-z0-9_]{20,}|github_pat_[A-Za-z0-9_]{20,}|(^|[^A-Za-z0-9_-])sk-[A-Za-z0-9][A-Za-z0-9_-]{20,}' \
  docs/development/adr/0015-notification-outbox-dedupe-policy.md
```

기대: 출력 없음.

- [ ] **Step 4: Slice commit**

```bash
git add docs/development/adr/0015-notification-outbox-dedupe-policy.md \
        server/src/main/kotlin/com/readmates/notification/application/service/NotificationEventService.kt
git commit -m "docs: pin notification outbox dedupe policy"
```

---

## Task 7: DEF-003 — route-guards clubSlug bypass 제거

**Files:**
- Modify: `front/src/app/route-guards.tsx`
- Test: `front/tests/unit/route-guards.test.tsx`(없으면 신규)

- [ ] **Step 1: 권한 매트릭스 단위 테스트 추가**

`front/tests/unit/route-guards.test.tsx`에 다음 시나리오를 추가:

```tsx
import { describe, expect, it } from "vitest";
import { MemoryRouter, Route, Routes } from "react-router";
import { render, screen } from "@testing-library/react";
import { AuthContext } from "@/src/app/auth-state";
import { RequireMemberApp, RequireHost } from "@/src/app/route-guards";

function renderWithAuth(auth: any, path: string, children: React.ReactNode) {
  return render(
    <AuthContext.Provider value={{ status: "ready", auth }}>
      <MemoryRouter initialEntries={[path]}>
        <Routes>
          <Route path="/clubs/:clubSlug/app/*" element={children} />
          <Route path="/login" element={<div>login</div>} />
          <Route path="/app" element={<div>app-root</div>} />
        </Routes>
      </MemoryRouter>
    </AuthContext.Provider>,
  );
}

describe("RequireMemberApp", () => {
  it("renders BlockedMemberApp for SUSPENDED member even when clubSlug is present", () => {
    renderWithAuth(
      { authenticated: true, membershipStatus: "SUSPENDED", approvalState: "APPROVED", role: "MEMBER" },
      "/clubs/foo/app/x",
      <RequireMemberApp>passed</RequireMemberApp>,
    );
    expect(screen.queryByText("passed")).toBeNull();
  });

  it("renders children for ACTIVE member with clubSlug", () => {
    renderWithAuth(
      { authenticated: true, membershipStatus: "ACTIVE", approvalState: "APPROVED", role: "MEMBER" },
      "/clubs/foo/app/x",
      <RequireMemberApp>passed</RequireMemberApp>,
    );
    expect(screen.getByText("passed")).toBeInTheDocument();
  });

  it("redirects unauthenticated even with clubSlug", () => {
    renderWithAuth(
      { authenticated: false, approvalState: "ANONYMOUS" },
      "/clubs/foo/app/x",
      <RequireMemberApp>passed</RequireMemberApp>,
    );
    expect(screen.getByText("login")).toBeInTheDocument();
  });
});
```

(`RequireHost`는 동일 패턴으로 3 시나리오 추가.)

- [ ] **Step 2: 테스트 실패 확인**

```bash
pnpm --dir front test -- route-guards
```

기대: SUSPENDED + clubSlug 시나리오 FAIL(현재는 bypass).

- [ ] **Step 3: route-guards.tsx 변경**

`front/src/app/route-guards.tsx`에서 두 가드 모두에서 `if (clubSlug) return <>{children}</>;` 줄 제거:

```tsx
export function RequireMemberApp({ children }: { children: ReactNode }) {
  const state = useAuth();
  const loginPath = useLoginPathForCurrentRoute();

  if (state.status === "loading") return <ReadmatesRouteLoading label="멤버 화면을 확인하는 중" variant="member" />;
  if (!state.auth.authenticated) return <Navigate to={loginPath} replace />;
  if (!canUseMemberApp(state.auth)) {
    return <BlockedMemberApp />;
  }
  return <>{children}</>;
}

export function RequireHost({ children }: { children: ReactNode }) {
  const state = useAuth();
  const { clubSlug } = useParams();
  const loginPath = useLoginPathForCurrentRoute();

  if (state.status === "loading") return <ReadmatesRouteLoading label="호스트 권한을 확인하는 중" variant="host" />;
  if (!state.auth.authenticated) return <Navigate to={loginPath} replace />;
  if (!canUseHostApp(state.auth)) return <Navigate to={scopedAppPath(clubSlug)} replace />;
  return <>{children}</>;
}
```

- [ ] **Step 4: 테스트 + lint + build 통과**

```bash
pnpm --dir front test -- route-guards
pnpm --dir front lint
pnpm --dir front build
```

기대: 모두 PASS.

- [ ] **Step 5: Slice commit**

```bash
git add front/src/app/route-guards.tsx front/tests/unit/route-guards.test.tsx
git commit -m "fix: enforce member-app guard regardless of club slug"
```

---

## Task 8: DEF-004 — auth-context 401 분리 + redirect guard

**Files:**
- Modify: `front/src/app/auth-state.ts`
- Modify: `front/src/app/auth-context.tsx`
- Modify: `front/shared/api/client.ts`
- Test: `front/tests/unit/auth-context.test.tsx`(확장)
- Test: `front/tests/unit/client-redirect-guard.test.ts`(신규)

- [ ] **Step 1: AuthState 확장**

`front/src/app/auth-state.ts`:

```ts
export type AuthState =
  | { status: "loading" }
  | { status: "ready"; auth: AuthMeResponse }
  | { status: "session_expired"; lastAuth?: AuthMeResponse };
```

- [ ] **Step 2: redirect-guard 테스트 추가(실패 재현)**

`front/tests/unit/client-redirect-guard.test.ts`:

```ts
import { describe, expect, it, beforeEach } from "vitest";
import { readmatesFetchResponse, __resetRedirectGuardForTest } from "@/shared/api/client";

describe("redirect guard", () => {
  beforeEach(() => {
    __resetRedirectGuardForTest();
    // window.location.assign mock
    delete (window as any).location;
    (window as any).location = { assign: vi.fn(), pathname: "/" };
  });

  it("redirects on first 401", async () => { /* ... */ });
  it("skips redirect on rapid second 401", async () => {
    // first 401 → redirect
    // second 401 within 1500ms → no redirect, throw ReadMatesSessionExpiredError
  });
  it("allows redirect again after cool-off", async () => { /* ... */ });
});
```

- [ ] **Step 3: 테스트 실패 확인**

```bash
pnpm --dir front test -- client-redirect-guard
```

기대: FAIL (`__resetRedirectGuardForTest` 미존재).

- [ ] **Step 4: client.ts 변경**

`front/shared/api/client.ts`에 가드 도입:

```ts
const REDIRECT_COOL_OFF_MS = 1500;
let lastLoginRedirectAt = 0;

export class ReadMatesSessionExpiredError extends Error {
  constructor() {
    super("ReadMates session expired");
    this.name = "ReadMatesSessionExpiredError";
  }
}

// test 전용 reset
export function __resetRedirectGuardForTest() {
  lastLoginRedirectAt = 0;
}

// readmatesFetchResponse 안의 401 분기
if (response.status === 401) {
  const now = Date.now();
  const inCoolOff = now - lastLoginRedirectAt < REDIRECT_COOL_OFF_MS;
  if (typeof window !== "undefined" && !inCoolOff) {
    lastLoginRedirectAt = now;
    window.location.assign(loginPathForReturnTo(currentRelativeReturnTo()));
  }
  throw new ReadMatesSessionExpiredError();
}
```

- [ ] **Step 5: auth-context 401 분기**

`front/src/app/auth-context.tsx`:

```ts
type FetchAuthMeOutcome =
  | { kind: "ok"; auth: AuthMeResponse }
  | { kind: "expired" }
  | { kind: "error" };

async function fetchAuthMeOutcome(): Promise<FetchAuthMeOutcome> {
  try {
    const response = await fetch("/api/bff/api/auth/me", { cache: "no-store" });
    if (response.status === 401) return { kind: "expired" };
    if (!response.ok) return { kind: "error" };
    return { kind: "ok", auth: (await response.json()) as AuthMeResponse };
  } catch {
    return { kind: "error" };
  }
}

// AuthProvider effect:
fetchAuthMeOutcome().then((outcome) => {
  if (cancelled || latestAuthRequestId.current !== requestId) return;
  if (outcome.kind === "ok") {
    setState({ status: "ready", auth: outcome.auth });
  } else if (outcome.kind === "expired") {
    setState({ status: "session_expired" });
  } else {
    setState({ status: "ready", auth: anonymousAuth });
  }
});
```

- [ ] **Step 6: 테스트 PASS 확인**

```bash
pnpm --dir front test -- auth-context
pnpm --dir front test -- client-redirect-guard
```

기대: 모두 PASS.

- [ ] **Step 7: Slice commit**

```bash
git add front/src/app/auth-state.ts front/src/app/auth-context.tsx front/shared/api/client.ts \
        front/tests/unit/auth-context.test.tsx front/tests/unit/client-redirect-guard.test.ts
git commit -m "fix: split 401 path and guard against redirect loops"
```

---

## Task 9: STRUCT-001 — SecretComparator 공통화

**Files:**
- Create: `server/src/main/kotlin/com/readmates/shared/security/SecretComparator.kt`
- Create: `server/src/test/kotlin/com/readmates/shared/security/SecretComparatorTest.kt`
- Modify: `server/src/main/kotlin/com/readmates/auth/infrastructure/security/BffSecretFilter.kt`
- Modify: `server/src/main/kotlin/com/readmates/auth/infrastructure/security/RateLimitFilter.kt`

- [ ] **Step 1: SecretComparatorTest 작성**

`server/src/test/kotlin/com/readmates/shared/security/SecretComparatorTest.kt`:

```kotlin
package com.readmates.shared.security

import org.assertj.core.api.Assertions.assertThat
import org.junit.jupiter.api.Test

class SecretComparatorTest {

    @Test
    fun `firstMatchingIndex returns -1 when no candidate matches`() {
        assertThat(SecretComparator.firstMatchingIndex("nope", listOf("a", "b", "c"))).isEqualTo(-1)
    }

    @Test
    fun `firstMatchingIndex returns the unique index when one candidate matches`() {
        assertThat(SecretComparator.firstMatchingIndex("b", listOf("a", "b", "c"))).isEqualTo(1)
    }

    @Test
    fun `matches returns true exactly when at least one candidate matches`() {
        assertThat(SecretComparator.matches("a", listOf("a"))).isTrue
        assertThat(SecretComparator.matches("z", listOf("a", "b"))).isFalse
    }

    @Test
    fun `empty candidate list returns -1 and matches false`() {
        assertThat(SecretComparator.firstMatchingIndex("x", emptyList())).isEqualTo(-1)
        assertThat(SecretComparator.matches("x", emptyList())).isFalse
    }
}
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
./server/gradlew -p server test --tests 'com.readmates.shared.security.SecretComparatorTest'
```

기대: 컴파일 에러(클래스 미존재).

- [ ] **Step 3: SecretComparator 작성**

`server/src/main/kotlin/com/readmates/shared/security/SecretComparator.kt`:

```kotlin
package com.readmates.shared.security

import java.nio.charset.StandardCharsets
import java.security.MessageDigest

object SecretComparator {
    fun firstMatchingIndex(provided: ByteArray, candidates: List<ByteArray>): Int {
        var matched = -1
        candidates.forEachIndexed { idx, candidate ->
            if (MessageDigest.isEqual(provided, candidate)) {
                matched = idx
            }
        }
        return matched
    }

    fun matches(provided: ByteArray, candidates: List<ByteArray>): Boolean =
        firstMatchingIndex(provided, candidates) >= 0

    fun firstMatchingIndex(provided: String, candidates: List<String>): Int =
        firstMatchingIndex(
            provided.toByteArray(StandardCharsets.UTF_8),
            candidates.map { it.toByteArray(StandardCharsets.UTF_8) },
        )

    fun matches(provided: String, candidates: List<String>): Boolean =
        firstMatchingIndex(provided, candidates) >= 0
}
```

- [ ] **Step 4: BffSecretFilter 사용처 교체**

`BffSecretFilter.kt`에서 `findMatchingSecretIndex` private 함수 제거. `aliasFor`를:

```kotlin
internal fun aliasFor(provided: String): String? =
    when (val idx = SecretComparator.firstMatchingIndex(provided, secrets)) {
        -1 -> null
        0 -> "primary"
        1 -> "secondary"
        else -> "index_$idx"
    }
```

- [ ] **Step 5: RateLimitFilter 사용처 교체**

`RateLimitFilter.kt`에서 `secretMatchesAny` private 함수 제거하고 호출처 line 162를:

```kotlin
if (!SecretComparator.matches(provided, trustedBffSecrets)) {
    return null
}
```

- [ ] **Step 6: 회귀 확인**

```bash
./server/gradlew -p server test \
  --tests 'com.readmates.shared.security.SecretComparatorTest' \
  --tests 'com.readmates.auth.infrastructure.security.BffSecretFilter*' \
  --tests 'com.readmates.auth.infrastructure.security.RateLimitFilter*'
rg -n 'MessageDigest.isEqual' server/src/main | wc -l
```

기대: 테스트 PASS. `MessageDigest.isEqual` 호출 위치가 `SecretComparator.kt` 1곳에만 남음(다른 위치가 있으면 검토 후 합치거나 명시 주석).

- [ ] **Step 7: Slice commit**

```bash
git add server/src/main/kotlin/com/readmates/shared/security/SecretComparator.kt \
        server/src/test/kotlin/com/readmates/shared/security/SecretComparatorTest.kt \
        server/src/main/kotlin/com/readmates/auth/infrastructure/security/BffSecretFilter.kt \
        server/src/main/kotlin/com/readmates/auth/infrastructure/security/RateLimitFilter.kt
git commit -m "refactor: extract timing-uniform secret comparator"
```

---

## Task 10: LOGIC-002 — MemberLifecycleStatus enum + 전이 검증

**Files:**
- Create: `server/src/main/kotlin/com/readmates/auth/domain/MemberLifecycleStatus.kt`
- Create: `server/src/test/kotlin/com/readmates/auth/domain/MemberLifecycleStatusTest.kt`
- Modify: `server/src/main/kotlin/com/readmates/auth/adapter/out/persistence/JdbcMemberLifecycleStoreAdapter.kt`

- [ ] **Step 1: enum 테스트 작성**

`MemberLifecycleStatusTest.kt`:

```kotlin
package com.readmates.auth.domain

import org.assertj.core.api.Assertions.assertThat
import org.junit.jupiter.api.Test

class MemberLifecycleStatusTest {

    @Test
    fun `ACTIVE allows SUSPENDED LEFT INACTIVE`() {
        assertThat(MemberLifecycleStatus.ACTIVE.allowsTransitionTo(MemberLifecycleStatus.SUSPENDED)).isTrue
        assertThat(MemberLifecycleStatus.ACTIVE.allowsTransitionTo(MemberLifecycleStatus.LEFT)).isTrue
        assertThat(MemberLifecycleStatus.ACTIVE.allowsTransitionTo(MemberLifecycleStatus.INACTIVE)).isTrue
    }

    @Test
    fun `SUSPENDED allows ACTIVE LEFT INACTIVE only`() {
        assertThat(MemberLifecycleStatus.SUSPENDED.allowsTransitionTo(MemberLifecycleStatus.ACTIVE)).isTrue
        assertThat(MemberLifecycleStatus.SUSPENDED.allowsTransitionTo(MemberLifecycleStatus.LEFT)).isTrue
        assertThat(MemberLifecycleStatus.SUSPENDED.allowsTransitionTo(MemberLifecycleStatus.INACTIVE)).isTrue
    }

    @Test
    fun `LEFT is terminal`() {
        MemberLifecycleStatus.values()
            .filter { it != MemberLifecycleStatus.LEFT }
            .forEach { target ->
                assertThat(MemberLifecycleStatus.LEFT.allowsTransitionTo(target)).isFalse
            }
    }

    @Test
    fun `fromStorage round-trips known values`() {
        MemberLifecycleStatus.values().forEach { value ->
            assertThat(MemberLifecycleStatus.fromStorage(value.storageValue)).isEqualTo(value)
        }
    }
}
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
./server/gradlew -p server test --tests 'com.readmates.auth.domain.MemberLifecycleStatusTest'
```

기대: 컴파일 에러.

- [ ] **Step 3: enum 작성**

`server/src/main/kotlin/com/readmates/auth/domain/MemberLifecycleStatus.kt`:

```kotlin
package com.readmates.auth.domain

enum class MemberLifecycleStatus(val storageValue: String) {
    ACTIVE("ACTIVE"),
    SUSPENDED("SUSPENDED"),
    LEFT("LEFT"),
    INACTIVE("INACTIVE");

    fun allowsTransitionTo(next: MemberLifecycleStatus): Boolean =
        TRANSITIONS[this].orEmpty().contains(next)

    companion object {
        private val TRANSITIONS: Map<MemberLifecycleStatus, Set<MemberLifecycleStatus>> = mapOf(
            ACTIVE to setOf(SUSPENDED, LEFT, INACTIVE),
            SUSPENDED to setOf(ACTIVE, LEFT, INACTIVE),
            LEFT to emptySet(),
            INACTIVE to emptySet(),
        )

        fun fromStorage(value: String): MemberLifecycleStatus =
            values().firstOrNull { it.storageValue == value }
                ?: throw IllegalArgumentException("Unknown member lifecycle status: $value")
    }
}

class IllegalMemberStateTransitionException(
    val from: MemberLifecycleStatus,
    val to: MemberLifecycleStatus,
) : IllegalStateException("Illegal member state transition: $from -> $to")
```

- [ ] **Step 4: 어댑터 사전 검증 추가**

`JdbcMemberLifecycleStoreAdapter.kt`의 `suspendActiveMember`/`restoreSuspendedMember`/`markMemberLeftByHost` 메서드 각각에서, 기존 SELECT(현재 상태 조회) 결과를 enum으로 변환하고 `allowsTransitionTo` 검증 후 SQL을 실행. 예:

```kotlin
val currentStatus = MemberLifecycleStatus.fromStorage(currentRow.status)
val nextStatus = MemberLifecycleStatus.SUSPENDED
if (!currentStatus.allowsTransitionTo(nextStatus)) {
    throw IllegalMemberStateTransitionException(currentStatus, nextStatus)
}
```

(현재 어댑터에 SELECT가 없으면, 한 번의 `SELECT ... FOR UPDATE`(또는 read-modify-write 단위) 추가.)

- [ ] **Step 5: 어댑터 통합 테스트 확장**

`JdbcMemberLifecycleStoreAdapterTest`에 다음 시나리오 추가(있으면 보강):

- LEFT 상태에서 `restoreSuspendedMember` 호출 → `IllegalMemberStateTransitionException`.
- INACTIVE에서 어떤 메서드도 통과 안 함.

- [ ] **Step 6: 회귀 확인**

```bash
./server/gradlew -p server test \
  --tests 'com.readmates.auth.domain.MemberLifecycleStatusTest' \
  --tests 'com.readmates.auth.adapter.out.persistence.JdbcMemberLifecycleStoreAdapterTest'
```

기대: 모두 PASS.

- [ ] **Step 7: Slice commit**

```bash
git add server/src/main/kotlin/com/readmates/auth/domain/MemberLifecycleStatus.kt \
        server/src/main/kotlin/com/readmates/auth/adapter/out/persistence/JdbcMemberLifecycleStoreAdapter.kt \
        server/src/test/kotlin/com/readmates/auth/domain/MemberLifecycleStatusTest.kt \
        server/src/test/kotlin/com/readmates/auth/adapter/out/persistence/JdbcMemberLifecycleStoreAdapterTest.kt
git commit -m "feat: pin member lifecycle state transitions"
```

---

## Task 11: STRUCT-002 — AuthoritySynthesisService 분리

**Files:**
- Create: `server/src/main/kotlin/com/readmates/auth/application/service/AuthoritySynthesisService.kt`(interface)
- Create: `server/src/main/kotlin/com/readmates/auth/application/service/DefaultAuthoritySynthesisService.kt`
- Create: `server/src/test/kotlin/com/readmates/auth/application/service/DefaultAuthoritySynthesisServiceTest.kt`
- Modify: `server/src/main/kotlin/com/readmates/auth/infrastructure/security/MemberAuthoritiesFilter.kt`

- [ ] **Step 1: service 단위 테스트 작성**

`DefaultAuthoritySynthesisServiceTest.kt`(전체):

```kotlin
package com.readmates.auth.application.service

import com.readmates.auth.application.model.SupportAccessGrant
import com.readmates.auth.domain.Member
import com.readmates.auth.domain.MemberLifecycleStatus
import com.readmates.club.adapter.`in`.web.ClubContextSource
import com.readmates.club.adapter.`in`.web.RequestedClubContext
import org.assertj.core.api.Assertions.assertThat
import org.junit.jupiter.api.Test
import org.springframework.security.core.authority.SimpleGrantedAuthority

class DefaultAuthoritySynthesisServiceTest {

    private val service = DefaultAuthoritySynthesisService()

    @Test
    fun `active member without host grant gets role member`() {
        val authorities = service.synthesize(
            member = activeMember(role = MemberRole.MEMBER),
            clubContext = slugContext(resolved = true),
            supportGrant = null,
        )
        assertThat(authorities).containsExactly(SimpleGrantedAuthority("ROLE_MEMBER"))
    }

    @Test
    fun `active host member gets role member plus role host`() {
        val authorities = service.synthesize(
            member = activeMember(role = MemberRole.HOST),
            clubContext = slugContext(resolved = true),
            supportGrant = null,
        )
        assertThat(authorities).containsExactlyInAnyOrder(
            SimpleGrantedAuthority("ROLE_MEMBER"),
            SimpleGrantedAuthority("ROLE_HOST"),
        )
    }

    @Test
    fun `slug supplied unknown club with platform admin grants only platform admin`() {
        val authorities = service.synthesize(
            member = null,
            clubContext = slugContext(resolved = false),
            supportGrant = SupportAccessGrant.platformAdmin(),
        )
        assertThat(authorities).contains(SimpleGrantedAuthority("ROLE_PLATFORM_ADMIN"))
        assertThat(authorities).doesNotContain(SimpleGrantedAuthority("ROLE_MEMBER"))
    }

    @Test
    fun `slug supplied unknown club with host support grant grants host`() {
        val authorities = service.synthesize(
            member = null,
            clubContext = slugContext(resolved = false),
            supportGrant = SupportAccessGrant.hostSupport(clubId = sampleClubId()),
        )
        assertThat(authorities).contains(SimpleGrantedAuthority("ROLE_HOST"))
    }

    @Test
    fun `host fallback unscoped yields empty`() {
        val authorities = service.synthesize(
            member = null,
            clubContext = hostFallbackContext(),
            supportGrant = null,
        )
        assertThat(authorities).isEmpty()
    }

    @Test
    fun `suspended member yields empty`() {
        val authorities = service.synthesize(
            member = activeMember(role = MemberRole.MEMBER, status = MemberLifecycleStatus.SUSPENDED),
            clubContext = slugContext(resolved = true),
            supportGrant = null,
        )
        assertThat(authorities).isEmpty()
    }

    private fun slugContext(resolved: Boolean): RequestedClubContext = /* helper */ TODO()
    private fun hostFallbackContext(): RequestedClubContext = /* helper */ TODO()
    private fun activeMember(role: MemberRole, status: MemberLifecycleStatus = MemberLifecycleStatus.ACTIVE): Member = /* helper */ TODO()
    private fun sampleClubId(): UUID = /* helper */ TODO()
}
```

위 helper 메서드(`slugContext`, `hostFallbackContext`, `activeMember`, `sampleClubId`)는 실제 도메인 객체 시그니처에 맞춰 채운다. 기존 `MemberAuthoritiesFilterTest` 또는 `AuthMeControllerTest` 안에 동등 helper가 있으면 그쪽에서 베껴 와 본 파일에 둔다(test fixture가 한 파일 안에 자족적이도록).

> 본 step의 의도는 service의 권한 매트릭스를 6 시나리오로 못 박는 것이다. helper의 시그니처는 실제 도메인 코드를 보고 채우되, **`TODO()` 자리는 반드시 실제 객체 생성으로 교체**한다(commit 전).

- [ ] **Step 2: 테스트 실패 확인**

```bash
./server/gradlew -p server test --tests '*AuthoritySynthesisService*'
```

기대: 컴파일 에러.

- [ ] **Step 3: 인터페이스 + 구현 작성**

`AuthoritySynthesisService.kt`:

```kotlin
package com.readmates.auth.application.service

import com.readmates.auth.application.model.SupportAccessGrant
import com.readmates.auth.domain.Member
import com.readmates.club.adapter.`in`.web.RequestedClubContext
import org.springframework.security.core.GrantedAuthority

interface AuthoritySynthesisService {
    fun synthesize(
        member: Member?,
        clubContext: RequestedClubContext,
        supportGrant: SupportAccessGrant?,
    ): Set<GrantedAuthority>
}
```

`DefaultAuthoritySynthesisService.kt`에 현재 filter 안의 분기 로직을 그대로 옮김. 권한 문자열 상수(`ROLE_MEMBER`, `ROLE_HOST`, `ROLE_PLATFORM_ADMIN`)는 service에 둠.

- [ ] **Step 4: MemberAuthoritiesFilter를 service 호출로 정리**

`MemberAuthoritiesFilter.kt`:

```kotlin
@Component
class MemberAuthoritiesFilter(
    private val memberLookup: ...,
    private val supportGrantLookup: CheckSupportAccessGrantUseCase,
    private val authoritySynthesisService: AuthoritySynthesisService,
) : OncePerRequestFilter() {

    override fun doFilterInternal(...) {
        val context = request.resolvedClubContext()
        val member = memberLookup.lookup(currentUser, context)
        val supportGrant = supportGrantLookup.check(currentUser)
        val authorities = authoritySynthesisService.synthesize(member, context, supportGrant)
        val principal = ...
        SecurityContextHolder.getContext().authentication = ReadmatesAuthentication(principal, authorities)
        filterChain.doFilter(request, response)
    }
}
```

filter 안에 `"ROLE_"` 리터럴이 더 이상 없도록 정리.

- [ ] **Step 5: 회귀 확인**

```bash
./server/gradlew -p server test \
  --tests '*AuthoritySynthesisService*' \
  --tests '*MemberAuthoritiesFilter*' \
  --tests '*AuthMeController*'
rg -n '"ROLE_' server/src/main/kotlin/com/readmates/auth/infrastructure/security/MemberAuthoritiesFilter.kt
```

기대: 테스트 모두 PASS. grep 결과 0개.

- [ ] **Step 6: Slice commit**

```bash
git add server/src/main/kotlin/com/readmates/auth/application/service/AuthoritySynthesisService.kt \
        server/src/main/kotlin/com/readmates/auth/application/service/DefaultAuthoritySynthesisService.kt \
        server/src/main/kotlin/com/readmates/auth/infrastructure/security/MemberAuthoritiesFilter.kt \
        server/src/test/kotlin/com/readmates/auth/application/service/DefaultAuthoritySynthesisServiceTest.kt \
        server/src/test/kotlin/com/readmates/auth/infrastructure/security/MemberAuthoritiesFilterTest.kt
git commit -m "refactor: move authority synthesis to application service"
```

---

## Task 12: STRUCT-003 — BFF secret rotation lifecycle (ADR-0014 + preflight)

**Files:**
- Create: `docs/development/adr/0014-bff-secret-rotation-lifecycle.md`
- Create: `front/functions/api/bff/__internal/secret-status.ts`
- Create: `front/tests/unit/cloudflare-bff-secret-status.test.ts`
- Modify: `front/functions/_shared/proxy.ts`(rotation stage + fingerprint helper)

- [ ] **Step 1: ADR-0014 작성**

`docs/development/adr/0014-bff-secret-rotation-lifecycle.md` 골자:

- Status: Accepted.
- Context: Cloudflare Pages edge 갱신 지연으로 BFF↔server skew 윈도우가 있고, 다중 secret 환경에서 회전 절차가 코드/문서에 명시 없음.
- Decision: 회전 4단계(staging → candidate → primary → retire) + server-first 원칙 + BFF에 `BFF_SECRET_ROTATION_STAGE` 환경 변수 도입(기본 `stable`).
- 표 1: 단계별 server/BFF 상태와 cutoff 권장.
- 표 2: `__internal/secret-status` 응답 의미.

- [ ] **Step 2: 진단 라우트 테스트 작성**

`front/tests/unit/cloudflare-bff-secret-status.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { onRequestGet } from "@/functions/api/bff/__internal/secret-status";

describe("BFF __internal/secret-status", () => {
  it("returns count + primary fingerprint for multi-secret env", async () => {
    const response = await onRequestGet({
      env: { READMATES_BFF_SECRETS: "alpha,bravo,charlie" },
    } as any);
    const body = await response.json();
    expect(body.configuredSecretCount).toBe(3);
    expect(body.rotationStage).toBe("stable");
    expect(body.primarySecretFingerprint).toMatch(/^[0-9a-f]{6}$/);
  });

  it("handles legacy single-secret env", async () => {
    const response = await onRequestGet({
      env: { READMATES_BFF_SECRET: "legacy" },
    } as any);
    const body = await response.json();
    expect(body.configuredSecretCount).toBe(1);
  });

  it("never exposes the raw secret", async () => {
    const response = await onRequestGet({
      env: { READMATES_BFF_SECRETS: "super-secret" },
    } as any);
    const text = await response.text();
    expect(text).not.toContain("super-secret");
  });
});
```

- [ ] **Step 3: 라우트 + helper 작성**

`front/functions/_shared/proxy.ts`에 추가(이미 secret 리스트 파싱 함수가 있음):

```ts
export function getRotationStage(env: { BFF_SECRET_ROTATION_STAGE?: string }): "stable" | "staging" {
  const raw = env.BFF_SECRET_ROTATION_STAGE?.trim().toLowerCase();
  return raw === "staging" ? "staging" : "stable";
}

export async function secretFingerprint(secret: string): Promise<string> {
  const data = new TextEncoder().encode(secret);
  const hash = await crypto.subtle.digest("SHA-256", data);
  const hex = Array.from(new Uint8Array(hash)).map((b) => b.toString(16).padStart(2, "0")).join("");
  return hex.slice(0, 6);
}
```

`front/functions/api/bff/__internal/secret-status.ts`:

```ts
import { getConfiguredBffSecrets, getRotationStage, secretFingerprint } from "@/functions/_shared/proxy";

export const onRequestGet: PagesFunction<{
  READMATES_BFF_SECRETS?: string;
  READMATES_BFF_SECRET?: string;
  BFF_SECRET_ROTATION_STAGE?: string;
}> = async (context) => {
  const secrets = getConfiguredBffSecrets(context.env);
  const primary = secrets[0];
  const body = {
    configuredSecretCount: secrets.length,
    rotationStage: getRotationStage(context.env),
    primarySecretFingerprint: primary ? await secretFingerprint(primary) : null,
  };
  return new Response(JSON.stringify(body), {
    headers: { "content-type": "application/json" },
  });
};
```

(`getConfiguredBffSecrets`는 기존 proxy.ts에 이미 비슷한 함수가 있다면 명칭 통일.)

- [ ] **Step 4: 테스트 통과 확인**

```bash
pnpm --dir front test -- cloudflare-bff-secret-status
```

기대: PASS.

- [ ] **Step 5: 공개 안전 스캔**

```bash
git diff --check -- docs/development/adr/0014-bff-secret-rotation-lifecycle.md \
        front/functions/api/bff/__internal/secret-status.ts \
        front/functions/_shared/proxy.ts \
        front/tests/unit/cloudflare-bff-secret-status.test.ts
rg -n '(/)Users/|readmates[.]kr|ocid1[.]|gh[pousr]_[A-Za-z0-9_]{20,}|github_pat_[A-Za-z0-9_]{20,}|(^|[^A-Za-z0-9_-])sk-[A-Za-z0-9][A-Za-z0-9_-]{20,}' \
  docs/development/adr/0014-bff-secret-rotation-lifecycle.md
```

기대: 모두 출력 없음.

- [ ] **Step 6: Slice commit**

```bash
git add docs/development/adr/0014-bff-secret-rotation-lifecycle.md \
        front/functions/api/bff/__internal/secret-status.ts \
        front/functions/_shared/proxy.ts \
        front/tests/unit/cloudflare-bff-secret-status.test.ts
git commit -m "feat: bff secret rotation lifecycle adr and preflight route"
```

---

## Task 13: LOGIC-005 — Deploy ledger NDJSON schema 정착

**Files:**
- Create: `docs/development/adr/0016-deploy-ledger-event-schema.md`
- Modify: `deploy/oci/05-deploy-compose-stack.sh`
- Modify: `deploy/oci/watch-compose-post-deploy.sh`
- Modify: `deploy/oci/01-vm-setup.sh`(jq 보장)
- Modify: `docs/operations/runbooks/deploy-attempts.md`(schema 인용)

- [ ] **Step 1: ADR-0016 작성**

`docs/development/adr/0016-deploy-ledger-event-schema.md` 골자:

- Status: Accepted.
- Context: ledger event 이름/세부가 같은 줄에 섞여 있어 기계 가독성 떨어짐.
- Decision: 한 줄 NDJSON schema `{"ts","stage","event","status","detail":{...}}`.
- 환경 변수 `READMATES_LEDGER_FORMAT`(`both|json|legacy`, 기본 `both`).
- 예시 변환표(legacy → json).

- [ ] **Step 2: jq 보장**

`deploy/oci/01-vm-setup.sh`의 패키지 설치 부분에 `jq` 추가:

```bash
sudo apt-get install -y jq
```

(이미 있으면 변경 없음.)

- [ ] **Step 3: helper 재정의**

`05-deploy-compose-stack.sh`의 `remote_ledger_append`를 다음 시그니처로:

```bash
remote_ledger_append() {
  local event="$1"
  local status="$2"
  local stage="$3"
  shift 3
  local detail
  detail=$(printf '%s\n' "$@" | jq -Rn '[inputs | split("=") | {(.[0]): (.[1:] | join("="))}] | add // {}')
  local ts
  ts="$(date -u +%Y-%m-%dT%H:%M:%SZ)"
  local format="${READMATES_LEDGER_FORMAT:-both}"

  if [ "$format" != "legacy" ]; then
    jq -nc \
      --arg ts "$ts" --arg stage "$stage" --arg event "$event" --arg status "$status" \
      --argjson detail "$detail" \
      '{ts:$ts, stage:$stage, event:$event, status:$status, detail:$detail}' \
      | _ledger_remote_append
  fi
  if [ "$format" != "json" ]; then
    local kvs="$*"
    printf '%s %s %s\n' "$event" "$status" "$kvs" | _ledger_remote_append
  fi
}
```

(`_ledger_remote_append`는 기존 SSH/sudo append 로직을 추출한 helper.)

- [ ] **Step 4: watch script 동일 helper 도입**

`watch-compose-post-deploy.sh`도 동일한 `remote_ledger_append`(혹은 inline echo)를 사용해 watch 단계 사건을 기록(예: `STAGE_STARTED post-deploy-watch`, `CHECK_FAILED post-deploy-watch reason=error-grep`).

- [ ] **Step 5: 셸 정적 검증 + manual fixture**

```bash
bash -n deploy/oci/05-deploy-compose-stack.sh deploy/oci/watch-compose-post-deploy.sh deploy/oci/01-vm-setup.sh
shellcheck deploy/oci/*.sh
READMATES_LEDGER_FORMAT=both bash -c 'source deploy/oci/05-deploy-compose-stack.sh:helpers && remote_ledger_append IMAGE_VERIFIED FAILED image-verify reason=running-image-id-empty'
```

(실제 SSH가 안 되는 환경에서는 `_ledger_remote_append` 대신 `tee /tmp/test-ledger` 같은 stub으로 분기해 fixture 확인.)

- [ ] **Step 6: 운영 런북 갱신**

`docs/operations/runbooks/deploy-attempts.md`에 신규 schema 예시 한 블록 추가, 기존 grep 패턴 + jq 파싱 예시 함께 안내.

- [ ] **Step 7: Slice commit**

```bash
git add docs/development/adr/0016-deploy-ledger-event-schema.md \
        deploy/oci/05-deploy-compose-stack.sh \
        deploy/oci/watch-compose-post-deploy.sh \
        deploy/oci/01-vm-setup.sh \
        docs/operations/runbooks/deploy-attempts.md
git commit -m "ops: ledger ndjson schema and dual-format writer"
```

---

## Task 14: STRUCT-004 — public-release-candidate 빌더 deny-list 우선화

**Files:**
- Modify: `scripts/build-public-release-candidate.sh`
- Modify: `scripts/verify-public-release-fixtures.sh`
- Create: `scripts/fixtures/public-release-candidate-coverage.txt`

- [ ] **Step 1: 빌더 deny-list 우선 모델로 정리**

`scripts/build-public-release-candidate.sh`에서:

1. `git ls-files -z | xargs -0 -n1 dirname | sort -u` 결과로 모든 top-level 경로를 수집(`approved manifest path`의 시작 집합).
2. `is_forbidden_candidate_path`에 추가 deny 패턴(있으면 보강):

```bash
case "$1" in
  node_modules|*/node_modules/*) return 0 ;;
  .git|*/.git/*) return 0 ;;
  .tmp|*/.tmp/*) return 0 ;;
  output|*/output/*) return 0 ;;
  *.env|*/credentials.json) return 0 ;;
  ...
esac
```

3. `is_approved_manifest_path`를 다음으로 단순화: deny에 걸리지 않은 경로는 모두 승인.

- [ ] **Step 2: coverage fixture 생성**

`scripts/fixtures/public-release-candidate-coverage.txt`에 현재 candidate top-level 디렉토리를 정렬해 기록:

```text
.github
CHANGELOG.md
README.md
compose.yml
deploy
docs
front
scripts
server
```

(`./scripts/build-public-release-candidate.sh` 1회 실행 → `ls .tmp/public-release-candidate | sort` 결과로 채움.)

- [ ] **Step 3: verify 스크립트에 diff 추가**

`scripts/verify-public-release-fixtures.sh` 끝에 추가:

```bash
candidate_top="$(ls .tmp/public-release-candidate | sort)"
expected_top="$(cat scripts/fixtures/public-release-candidate-coverage.txt)"
if [ "$candidate_top" != "$expected_top" ]; then
  echo "public release candidate top-level mismatch:" >&2
  diff <(echo "$expected_top") <(echo "$candidate_top") >&2 || true
  exit 1
fi
```

- [ ] **Step 4: 셸 정적 검증**

```bash
bash -n scripts/build-public-release-candidate.sh scripts/verify-public-release-fixtures.sh
shellcheck scripts/build-public-release-candidate.sh scripts/verify-public-release-fixtures.sh
```

기대: 출력 없음.

- [ ] **Step 5: 빌더 + 검증 통합 실행**

```bash
./scripts/build-public-release-candidate.sh
./scripts/verify-public-release-fixtures.sh
./scripts/public-release-check.sh .tmp/public-release-candidate
```

기대: 모두 PASS.

- [ ] **Step 6: Slice commit**

```bash
git add scripts/build-public-release-candidate.sh \
        scripts/verify-public-release-fixtures.sh \
        scripts/fixtures/public-release-candidate-coverage.txt
git commit -m "ops: deny-list driven public release candidate manifest"
```

---

## Task 15: Full Verification

**Files:**
- Verify all modified files.

- [ ] **Step 1: 셸 syntax/lint 전체**

```bash
bash -n scripts/*.sh deploy/oci/*.sh
shellcheck scripts/*.sh deploy/oci/*.sh
```

기대: 출력 없음. shellcheck 미설치이면 사유 기록.

- [ ] **Step 2: 서버 전체 테스트**

```bash
./server/gradlew -p server clean test
```

기대: 통과. (사전 환경 이슈인 `RedisAuthSessionCacheAdapterTest` 2건 실패는 별도 follow-up. Skipped로 보고.)

- [ ] **Step 3: 프론트 lint/test/build**

```bash
pnpm --dir front lint
pnpm --dir front test
pnpm --dir front build
```

기대: 모두 통과.

- [ ] **Step 4: E2E**

```bash
pnpm --dir front test:e2e
```

기대: 통과. 환경 미비로 실행 불가하면 Skipped로 명시.

- [ ] **Step 5: 공개 release 안전**

```bash
./scripts/build-public-release-candidate.sh
./scripts/public-release-check.sh .tmp/public-release-candidate
./scripts/verify-public-release-fixtures.sh
```

기대: 모두 통과.

- [ ] **Step 6: ArchUnit 회귀**

```bash
./server/gradlew -p server test --tests 'com.readmates.architecture.ServerArchitectureBoundaryTest'
```

기대: PASS. baseline exception 5건은 변경 없음.

- [ ] **Step 7: 문서 whitespace + 공개 안전**

```bash
git diff --check -- CHANGELOG.md \
  docs/superpowers/specs/2026-05-12-v1.7-defects-and-structural-cleanup-detailed-implementation.md \
  docs/superpowers/plans/2026-05-12-v1.7-defects-and-structural-cleanup-implementation-plan.md \
  docs/development/adr/0014-bff-secret-rotation-lifecycle.md \
  docs/development/adr/0015-notification-outbox-dedupe-policy.md \
  docs/development/adr/0016-deploy-ledger-event-schema.md
rg -n '(/)Users/|readmates[.]kr|ocid1[.]|gh[pousr]_[A-Za-z0-9_]{20,}|github_pat_[A-Za-z0-9_]{20,}|(^|[^A-Za-z0-9_-])sk-[A-Za-z0-9][A-Za-z0-9_-]{20,}' \
  CHANGELOG.md \
  docs/superpowers/specs/2026-05-12-v1.7-defects-and-structural-cleanup-detailed-implementation.md \
  docs/superpowers/plans/2026-05-12-v1.7-defects-and-structural-cleanup-implementation-plan.md \
  docs/development/adr/0014-bff-secret-rotation-lifecycle.md \
  docs/development/adr/0015-notification-outbox-dedupe-policy.md \
  docs/development/adr/0016-deploy-ledger-event-schema.md
```

기대: 모두 출력 없음.

- [ ] **Step 8: diff scope 점검**

```bash
git diff --stat 55e1be1..HEAD
```

기대: 본 plan File Map에 등장하는 파일들에 한정. 무관 변경이 섞여 있지 않음.

- [ ] **Step 9: CHANGELOG `Unreleased` 갱신**

다음을 `## Unreleased` 아래 적절한 카테고리(Fixed/Added/Changed/Deployment Notes)에 추가:

- **Fixed**: BFF audit executor가 graceful shutdown에서 진행/대기 큐를 비울 때까지 대기합니다.
- **Fixed**: 운영 환경에서 `READMATES_IP_HASH_BASE_SECRET`이 비어 있으면 startup이 명시적 메시지와 함께 실패합니다.
- **Fixed**: 클럽 컨텍스트가 있어도 멤버 앱/호스트 화면 진입 시 권한이 검증됩니다.
- **Fixed**: 세션 만료(401) 후 짧은 시간 안의 재진입에서 무한 리다이렉트가 차단되고, 사용자에게 세션 만료 상태가 노출됩니다.
- **Fixed**: Email delivery의 UNKNOWN 상태는 재시도 가능 예외로 분류됩니다.
- **Added**: 신규 메트릭 `bff.audit.shutdown.dropped`, `notification.dispatch.unknown_status`.
- **Added**: BFF 진단 라우트 `GET /api/bff/__internal/secret-status`(secret 자체는 노출하지 않음).
- **Added**: ADR-0014/0015/0016(BFF secret rotation, notification dedupe, deploy ledger schema).
- **Changed**: BFF/RateLimit 시크릿 비교가 공통 `SecretComparator`로 모입니다.
- **Changed**: 권한 합성이 application service(`AuthoritySynthesisService`)로 분리됩니다.
- **Changed**: deploy ledger가 NDJSON + legacy 두 라인을 기본으로 기록합니다(`READMATES_LEDGER_FORMAT`).
- **Changed**: 공개 release 후보 빌더가 deny-list 우선 모델로 정렬됩니다. 새 top-level 디렉토리는 fixture를 갱신해야만 통과합니다.
- **Deployment Notes**: 신규 환경 변수 — `READMATES_LEDGER_FORMAT`(기본 `both`), `BFF_SECRET_ROTATION_STAGE`(기본 `stable`). 기존 `READMATES_IP_HASH_BASE_SECRET`은 운영 환경에서 필수.

```bash
git add CHANGELOG.md
git commit -m "docs: capture defects-and-structural-cleanup unreleased entry"
```

---

## Task 16: Release Readiness

**Files:**
- No required code files.
- Optional PR body or handoff note.

- [ ] **Step 1: 환경 의존 검사 한계 기록**

shellcheck, actionlint, E2E 등 환경 의존 검사가 실행되지 않았으면 PR 본문에 다음 형식으로 적는다.

```text
Skipped: actionlint, because it is not installed in the local environment.
```

기대: skipped 검사를 passed로 표현하지 않는다.

- [ ] **Step 2: 무관 변경 점검**

```bash
git status --short --branch
git log --oneline 55e1be1..HEAD
```

기대: 본 plan 외 파일이 commit에 섞여 있지 않다.

- [ ] **Step 3: PR 요약 준비**

```markdown
## Summary
- BFF audit executor가 graceful shutdown에서 큐를 비우고, 누락 시 메트릭으로 노출됩니다.
- 운영 환경의 `READMATES_IP_HASH_BASE_SECRET` 누락이 startup 실패로 드러납니다.
- 멤버 앱/호스트 화면 권한 가드가 `clubSlug` 유무와 관계없이 검증을 실행합니다.
- 세션 만료 상태가 명시 상태(`session_expired`)로 분리되고 빠른 401 연쇄에서 무한 리다이렉트가 차단됩니다.
- Email delivery UNKNOWN 상태가 retryable로 분류됩니다.
- BFF/RateLimit 시크릿 비교가 `SecretComparator`로 모이고, 권한 합성이 `AuthoritySynthesisService`로 분리됩니다.
- ADR-0014/0015/0016 정착(BFF secret rotation, notification dedupe, deploy ledger schema).
- 공개 release 후보 빌더가 deny-list 우선 모델 + coverage fixture로 누락을 잡습니다.

## Checks
- ./server/gradlew -p server clean test
- pnpm --dir front lint
- pnpm --dir front test
- pnpm --dir front build
- pnpm --dir front test:e2e
- ./scripts/build-public-release-candidate.sh
- ./scripts/public-release-check.sh .tmp/public-release-candidate
- ./scripts/verify-public-release-fixtures.sh
- bash -n scripts/*.sh deploy/oci/*.sh
- shellcheck scripts/*.sh deploy/oci/*.sh
- actionlint .github/workflows/*.yml
```

- [ ] **Step 4: PR 분할 결정(선택)**

13개 슬라이스 + 베이스라인이 한 PR로 묶기엔 큽니다. 검토 부담을 줄이려면 다음 분할을 권장:

- PR-1 (Phase 1·2): Task 2~8 — 보안/감사 무결성 + 클라이언트 가드.
- PR-2 (Phase 3): Task 9~10 — 시크릿 비교 helper + 멤버 lifecycle enum.
- PR-3 (Phase 4): Task 11~14 — 권한 합성 service + BFF rotation ADR + ledger schema + release manifest.

각 PR이 독립적으로 통과 가능하도록 본 plan을 따른다.

기대: PR 요약이 13개 슬라이스와 실제 실행한 검사 목록을 반영한다.
